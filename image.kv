<SmartTileFromURL@MDSmartTile>
    # You can add box_color, radius, etc. if you want
        source: root.source
        size_hint: 1, None
        radius: self.width *.05
        height: self.width
        box_color: 0,0,0,0
